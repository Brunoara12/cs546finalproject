<div class="container">
    <div id="carouselExampleControls" class="carousel slide margin-bottom" data-ride="carousel">
        <div class="carousel-inner">
            {{#each activities}}
                {{#if @first}}
                    <div class="carousel-item active">
                {{else}}
                    <div class="carousel-item">
                {{/if}}
                        <div class="card" >
                            <div class="card-body">
                                <h5 class="card-title">Check out Where others have been!</h5>
                                <p class="card-text">Some user has been to {{this.location}} on {{this.date}}.</p>
                            </div>
                        </div>
                    </div>
            {{/each}}
            
        </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
    <div class="row">
        <div class="col-sm">
            <div class="card" >
                <div class="card-body">
                    <h5 class="card-title">About The Website</h5>
                    <p class="card-text">This web application allows users to register an account, login and log activities that they do. Currently the app allows for logging of Downhill skiing, Cross Country Skiing and Running.</p>
                </div>
            </div>
        </div>
    </div>
</div>
